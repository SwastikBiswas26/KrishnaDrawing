## Prerequisites & Dependencies

Make sure you have Python installed, then install the required libraries by running the following command in your terminal:

```bash
pip install opencv-python Pillow numpy pilmoji

```
